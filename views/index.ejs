<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="/javascripts/index.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" type="text/css" href="/stylesheets/style.css">
    <link rel="icon" href="/images/yad2Logo.png" type="image/png">
    <title><%= title %></title>
</head>
<body>
<div class="container-fluid">
    <div class="row">
        <div class="col-2 mt-4">
            <form action="/login" method="get" class="mb-3">
                <button type="submit" class="btn btn-primary btn-lg">Admin Area</button>
            </form>
        </div>
        <div class="col-2 mt-4">
            <form action="/new-ad" method="get">
                <button type="submit" class="btn btn-success btn-lg">Post New Ad</button>
            </form>
        </div>
        <div class="col mb-4 mt-4 ml-2">
            <img src="/images/yad2Logo.png">
        </div>
    </div>
    <div class="row">
        <div class="col">
            <form action="/" method="get" class="mb-3">
                <div class="input-group">
                    <input type="text" class="form-control" name="search" placeholder="Search by title..." value="<%= searchQuery %>">
                    <button type="submit" class="btn btn-success">Search</button>
                    <button type="button" class="btn btn-danger" onclick="clearSearch()">Clear</button>
                </div>
            </form>
        </div>
    </div>
    <div class="row">
        <% ads.forEach((ad, index) => { %>
            <div class="col-lg-4 col-md-6 col-sm-6 mb-4 ">
                <div class="card" style="width: 18rem;">
                    <ul class="list-group list-group-flush">
                        <li class="list-group-item"><strong>Title:</strong> <%= ad.title %></li>
                        <li class="list-group-item"><strong>Email:</strong> <%= ad.email %></li>
                        <li class="list-group-item"><strong>Price:</strong> <%= ad.price %></li>
                        <% if (ad.phoneNumber) { %>
                            <li class="list-group-item"><strong>Phone:</strong> <%= ad.phoneNumber %></li>
                        <% } %>
                        <% if (ad.description) { %>
                            <li class="list-group-item"><strong>Description:</strong> <%= ad.description %></li>
                        <% } %>
                    </ul>
                    <div class="card-footer text-center">
                        <p class="mb-0"><%= index + 1 %></p>
                    </div>
                </div>
            </div>
        <% }); %>
    </div>
</div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-VZxEAgz5NDBW6P/P2Q6U72dyJlRUAtvQ/GPw5N2H6FfWU1zj3eIlWbVvK3/dF3c" crossorigin="anonymous"></script>
</body>
</html>
